name: 🚀 Deploy website on push
on: push
jobs:
  build:
    name: 🎉 Deploy
    runs-on: self-hosted
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
    - name: NPM Version 16
    - uses: actions/setup-node@v3
      with:
        node-version: '16'
    - run: yarn
    
